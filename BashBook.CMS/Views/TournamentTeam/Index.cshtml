@model IEnumerable<BashBook.DAL.EDM.TournamentTeam>

<div class="row" style="text-align: center">
    <div class="col-sm-6">
        <h2>Tournament Teams</h2>
    </div>
    <div class="col-sm-6">
        @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-info" })
    </div>
</div>

<table class="table">
    <tr>
        <th>
            Team
        </th>
        <th>
            Tournament
        </th>
        <th>
            Actions
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Team.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tournament.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TournamentTeamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TournamentTeamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TournamentTeamId })
        </td>
    </tr>
}

</table>
